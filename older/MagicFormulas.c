#include <stdio.h>

int main(void)
{
	int n, ans = 0;
	
	scanf("%d", &n);
	for (i = 0; i < n; ++i)
	{
		int x;
		
		scanf("%d", &x);
		ans ^= x;
	}
	for (i = 1; i <= n; i++)
	{
		for (j = 1; j < n; j++)
		{
		
		}
	}
	return 0;
}
